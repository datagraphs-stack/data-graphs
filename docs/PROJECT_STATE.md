# Project state — 2026-09-07

## Operational truth

Phase A and meaningful parts of B–G exist: a dependency-free static web Studio, robust basic CSV parser, conservative profile, a versioned canonical metric semantic, versioned constrained AnalysisPlan, structural and semantic validation, fixed-point decimal aggregation, deterministic comparison, explicit clarification, and execution-derived Verify evidence. The Studio now operates on a versioned serializable DataGraph envelope with one entity ID, exact source bytes/hash, profile and semantic hashes, content-addressed revisions/results, explicit parent lineage, plans, interpretations, traces, and visualization specifications. Tests prove exact round-trip recomputation and detect source, profile, semantic, result, and lineage tampering.

Workers AI now proposes bounded analytical intent slots from the question, conservative structure, declared semantics, and prior revision context without receiving source rows. Deterministic code compiles and validates those untrusted slots, rejects extra fields, and blocks material ambiguity before the engine can execute. Provider/model/prompt metadata and the slot proposal enter revision provenance; model values cannot become results. A global D1 hourly budget protects staging inference spend, but there is still no application authentication or private access control. D1 persistence, stable `/g/<dataGraphId>` routes, exact-envelope reload, immutable revision append, creator write tokens, deletion API, and fresh-browser public-link sharing are staging-verified. Historical reload renders stored results without recomputation. Currency stays unknown; cancelled/refunded policy is explicit. Average and percentage outputs still need an explicit rounding policy. Production and staging run merged commit `7ba7604`; `datagraphs.ai`, `app.datagraphs.ai`, and `staging.datagraphs.ai` all serve the Studio. Public-link storage exposes exact source bytes to anyone with the URL and is disclosed through a required acknowledgment before first save; it is not private sharing. Cloudflare has isolated staging and production Pages projects and D1 databases; `staging.datagraphs.ai` targets staging, while `datagraphs.ai` and `app.datagraphs.ai` target production.

## MVP distance and exact next slice

This work is `advancing_core`: the exact merged staging release now proves the complete canonical loop from SOURCE → SEMANTICS → MODEL-PROPOSED INTENT → VALIDATED PLAN → DETERMINISTIC COMPUTATION → EVIDENCE → PRESENTATION → REVISION → PERSISTENCE → RELOAD/SHARE. This is the founding proof, not yet evidence of broad dataset/question compatibility or external-user product fit.

Active dependency sequence: (1) semantic and ambiguity correction **complete**; (2) execution-derived evidence trace **complete and staging-verified**; (3) DataGraph envelope and identities **complete and staging-verified**; (4) persistence/reload/share **complete and staging-verified**; (5) bounded AI intent proposal **complete and staging-verified**; (6) complete stranger/browser founding proof **complete on the exact merged production release**. The first perfect canonical DataGraph is therefore achieved. External-user hardening is also merged and production-verified: deterministic compatibility preflight blocks unsupported schemas, first publication requires explicit exact-source/public-link acknowledgment, provider outages fail closed with recovery text, and missing creator tokens are explicitly read-only. A real independent stranger usability session is the remaining evidence gate before an external invitation; it cannot be honestly substituted with another agent-authored browser script.

## Runtime verification status

- **local browser:** Chromium loaded the fixture, computed revision 1, selected the bounded follow-up, computed revision 2, opened Verify, found three ranked rows, and emitted no page errors.
- **persistence staging proof:** merged commit `1f3f19f` established the D1 persistence boundary. Chromium created and saved a two-revision DataGraph, closed its creator context, reopened the stable route in a fresh context, preserved exact source and result identities, rendered the stored historical result read-only without recomputation, and emitted no page errors. The proving share remains available at `/g/dg_c96a717a-381f-43e6-8b0b-4d55b0a07cd5` on the staging origin.
- **AI preview:** a real CSV upload produced revision 1 through Workers AI → deterministic slot compilation → validation → engine execution. The model-proposed annual follow-up stopped for three-way grain clarification before execution. After confirmation, revision 2 saved, reloaded in a fresh context with matching source/result identities, and rendered read-only from stored history with no page errors.
- **merged founding proof:** Chromium repeated the entire workflow against merged commit `fcadb4a`: actual file upload; accurate 13-row/11-column profile; Workers AI proposal; deterministic compilation/validation/computation; explicit three-choice ambiguity stop; confirmed revision 2; server save; closed creator context; stable-route reload with matching source/result identities; read-only stored historical result; zero page errors. The durable proof is `/g/dg_a9ece9eb-b90e-402b-ba92-39be3d4f01ee`.
- **discovered and corrected:** the static app previously imported CSS as a JavaScript module, which native browsers reject. The stylesheet is now loaded from `index.html`; the browser smoke is the first direct evidence that the runnable shell executes rather than merely builds.

GitHub repository access and Cloudflare account/project authority were verified without exposing credential values. Portfolio HQ was not modified. Git history plus this state file provide reconstruction evidence.
