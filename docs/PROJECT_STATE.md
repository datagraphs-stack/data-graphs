# Project state — strategic reset, 9 September 2026

## Current direction

The founder has explicitly rejected CSV analytics as the business. The active mission is permitted internet collection -> maintained entity/relationship graphs -> intelligence products. The CSV Studio may stay online as a demonstration. The strategy is reset; the backend has not been rebuilt by editing these documents.

## Implementation baseline

The main branch inspected for this reset was `979b64eb1a21bd5a43c7ee5ae9dbc92838de80e5`, dated 7 September 2026. It contains the existing CSV Studio, bounded AI intent endpoint, versioned analytical envelopes, D1 persistence, and recorded production/staging deployment receipts.

Those components are reusable demonstration infrastructure, not a delivered web-collection or knowledge-graph engine. The prior state report's completed 'first perfect DataGraph' refers only to its canonical CSV proof. It does not establish completion of the founder's web-data mission.

The full previous state is preserved at `history/pre-reset-2026-09-09/PROJECT_STATE.md`. Recorded deployment details remain in `DEPLOY_ORIGIN.md`; no fresh deployment or complete end-to-end runtime verification was performed as part of this documentation reset.

## Current graph-business gaps

No implemented crawler, source registry/frontier, cross-source entity resolution, evidence-backed web-claim graph, automated recrawl/change pipeline, or paid intelligence product is established by the inspected baseline. Treat each as work to implement and verify, not as completed capabilities. The first market, validated pricing, paying customers, and actual domain/infrastructure costs remain unverified.

Known demo limitations remain relevant to its safe operation: public-link source exposure; no private read-access accounts; the persistence path validates content consistency but does not independently recompute submitted results. Do not claim hashes alone prove truth or correctness. Address urgent safety defects without turning demo hardening into the new commercial roadmap.

## Next slice

Follow `ROADMAP.md`: define a provisional source and buyer contract, then build one bounded collection/evidence path and the first cross-source entity relationships. Prove a recrawl-driven intelligence change. No private CSV SaaS, chart redesign, or broader upload compatibility is a prerequisite.

## Scope and verification of this reset

Documentation changes only. Original planning documents are retained byte-for-byte under `history/pre-reset-2026-09-09/`. No application code, migrations, database contents, Cloudflare configuration, secrets, or production deployments are changed.

The existing tests were not rerun for this documentation-only change; an attempted download of the public source archive into the local runtime failed at DNS resolution. No test or runtime success is claimed. A future code task must obtain the worktree and run the existing checks plus new collection/graph tests.

This state describes the reset change set. Its availability on main depends on the actual branch/PR merge state; verify that state rather than assuming this review change has shipped.
